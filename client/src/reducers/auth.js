import moment from 'moment';

const initialState = {
}

export default function(state = initialState , action){
  // if(action.type === 'SET_EXPIRE_CONFIRM_CODE'){
  //   return Object.assign({} , state, { expireConfirmCode: moment.now() });
  // }

  return state;
}